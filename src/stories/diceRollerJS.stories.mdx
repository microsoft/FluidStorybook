import { FluidLoader } from '../fluidRendering/fluidLoader';
import { DiceRollerContainer } from '../components/diceRollerJS/DiceRollerContainer';
import { DiceRollerView } from '../components/diceRollerJS/DiceRollerView';

<Meta title="JS Demos/Dice Roller JS" component={FluidLoader} />

# Dice Roller JS

This example demonstrates how to create a dice roller data object. It allows multiple
users to roll a die in real-time and show the result to all collaborators. This example 
only uses JavaScript and the DOM to render the output.

Key Distributed Data Structures (DDSs) used include:

* SharedDirectory

<Story name="Demo">
    <FluidLoader factory={DiceRollerContainer} view={DiceRollerView} viewType="js"></FluidLoader>
</Story>